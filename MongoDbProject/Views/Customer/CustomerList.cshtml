@model List<MongoDbProject.Dtos.CustomerDto.ResultCustomerDto>
@{
    ViewData["Title"] = "ProductList";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}
<div class="row">
    <div class="col">
        <div class="card shadow">
            <div class="card-header border-0">
                <h3 class="mb-0">Müşteri Listesi</h3>
            </div>
            <div class="table-responsive">
                <table class="table align-items-center table-flush">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Müşteri Adı Soyadı </th>
                            <th scope="col">Müşteri Adres </th>
                            <th scope="col">Müşteri Telefon </th>
                            <th scope="col">Sil</th>
                            <th scope="col">Güncelle</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>

                                <td>
                                    @item.CustomerFullName
                                </td>
                                <td>
                                    @item.CustomerAddress
                                </td>
                                <td>
                                    @item.CustomerPhone
                                </td>


                                <td><a href="/Customer/RemoveCustomer/@item.CustomerId" class="btn btn-warning">Sil</a></td>
                                <td><a href="/Customer/UpdateCustomer/@item.CustomerId" class="btn btn-info">Güncelle</a></td>
                            </tr>
                        }
                    </tbody>
                </table>

            </div>
        </div>
        <br />
        <td><a href="/Customer/CreateCustomer/" class="btn btn-primary">Yeni Müşteri Girişi</a></td>
    </div>
</div>